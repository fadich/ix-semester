/* is.c                         C-UNIX                16.02.93
 All "is_something" of H. Schildt interpreter
*/
char isdelim (c)
    char c;
{
  if (strchr(" ;,+-<>/*%=()", c) || c == 9 ||
         c == '\n' || c == '\r' || c == 0)
                              return 1;
                              return 0;
}
/* ------------------------------------------------------------------ */
char  iswhite (c)
     char c;
{
   if (c == ' ' || c == '\t') return 1;
                              return 0;
}
/* -------------------------------------------------------------------- */
/*char  isdigit (c)
     char c;
{
  if (c <= '9'  &&  c >= '0')  return 1;
                               return 0;
} */
/* -------------------------------------------------------------------- */
/*char  isalpha (c)
     char c;
{
  if ( (c >= 'A' && c <= 'Z')  ||
       (c >= 'a' && c <= 'z') )  return 1;
                                 return 0;
} */
/* ------------------------------------------------------------------- */
/*char  tolower (c)
     char c;
{
  if ( c >= 'A' && c <= 'Z')
       return ( c - 'A' + 'a' );
       return c;
} */
/* ----------------------------------------------------------------- */
/*char  toupper (c)
     char c;
{
  if ( c >= 'a'  && c <= 'z' )
       return ( c - 'a' + 'A' );
       return c;
} */
/* is.c ------------------------------------------------------------- */
